title: Blocks
extends: pages/default
fields:
  blocks: true

  nestedStructure:
    label: Nested Blocks
    type: blocks
    fieldsets:
      test:
        fields:
          blocks: true

  fields:
    type: blocks
    fieldsets:
      test:
        wysiwyg: true
        preview: fields
        fields:
          title:
            type: text
          text:
            type: writer

  nestedBlocks:
    label: Nested Blocks
    type: blocks
    fieldsets:
      test:
        wysiwyg: true
        preview: fields
        fields:
          blocks:
            type: blocks
