title: Blocks
extends: pages/default

tabs:
  simple:
    fields:
      blocks: true
      disabled:
        type: blocks
        disabled: true
      tabs:
        type: blocks
        fieldsets:
          - tabs
      fields:
        type: blocks
        fieldsets:
          test:
            wysiwyg: true
            preview: fields
            fields:
              title:
                type: text
              text:
                type: writer


  nested:
    fields:
      nestedStructure:
        label: Nested Blocks
        type: blocks
        fieldsets:
          test:
            fields:
              blocks: true
      nestedBlocks:
        label: Nested Blocks
        type: blocks
        fieldsets:
          test:
            wysiwyg: true
            preview: fields
            fields:
              blocks:
                type: blocks
