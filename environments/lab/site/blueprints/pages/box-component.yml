title: Box

tabs:
  variants:
    sections:
      align:
        type: playground
        label: "Alignment"
        components:
          - component: k-box
            props:
              text: Left (default)
              theme: empty
          - component: k-box
            props:
              text: Center
              align: center
              theme: empty
      height:
        type: playground
        label: "height"
        components:
          - component: k-box
            props:
              text: default
              theme: notice
          - component: k-box
            props:
              text: 5rem
              theme: notice
              height: 5rem
      html:
        type: playground
        label: "HTML"
        components:
          - component: k-box
            props:
              text: Without <strong>HTML support</strong>
              theme: info
          - component: k-box
            props:
              text: With <strong>HTML support</strong>
              theme: info
              html: true
  themes:
    sections:
      themes:
        type: playground
        label: "Text"
        components:
          - component: k-box
            props:
              text: negative
              theme: negative
          - component: k-box
            props:
              text: notice
              theme: notice
          - component: k-box
            props:
              text: warning
              theme: warning
          - component: k-box
            props:
              text: positive
              theme: positive
          - component: k-box
            props:
              text: info
              theme: info
          - component: k-box
            props:
              text: none
          - component: k-box
            props:
              text: empty
              theme: empty
          - component: k-box
            props:
              text: white
              theme: white
          - component: k-box
            props:
              text: text
              theme: text
      themesIcon:
        type: playground
        label: "Text & Icon"
        components:
          - component: k-box
            props:
              icon: circle
              text: negative
              theme: negative
          - component: k-box
            props:
              icon: circle
              text: notice
              theme: notice
          - component: k-box
            props:
              icon: circle
              text: warning
              theme: warning
          - component: k-box
            props:
              icon: circle
              text: positive
              theme: positive
          - component: k-box
            props:
              icon: circle
              text: info
              theme: info
          - component: k-box
            props:
              icon: circle
              text: none
          - component: k-box
            props:
              icon: circle
              text: empty
              theme: empty
          - component: k-box
            props:
              icon: circle
              text: white
              theme: white
          - component: k-box
            props:
              icon: circle
              text: text
              theme: text
