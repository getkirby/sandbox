title: Pages

tabs:
  list:
    sections:
      list:
        type: pages
        template: page
      listWithInfo:
        type: pages
        info: "{{ page.slug }}"
        template: page
      listWithoutImage:
        type: pages
        layout: list
        image: false
        template: page
      listWithSearch:
        type: pages
        template: page
        search: true

  cardlets:
    sections:
      cardlets:
        type: pages
        layout: cardlets
        template: page
      cardletsWithInfo:
        type: pages
        layout: cardlets
        info: "{{ page.slug }}"
        template: page
      cardletsWithoutImage:
        type: pages
        layout: cardlets
        image: false
        template: page
      cardletsWithSearch:
        type: pages
        layout: cardlets
        template: page
        search: true

  cards:
    sections:
      cards:
        type: pages
        layout: cards
        template: page
      cardsWithInfo:
        type: pages
        layout: cards
        info: "{{ page.slug }}"
        template: page
      cardsWithoutImage:
        type: pages
        layout: cards
        image: false
        template: page
      cardsWithSearch:
        type: pages
        layout: cards
        template: page
        search: true

  table:
    sections:
      table:
        type: pages
        layout: table
        template: page
      tableWithInfo:
        type: pages
        layout: table
        info: "{{ page.slug }}"
        template: page
      tableWithoutImage:
        type: pages
        layout: table
        image: false
        template: page
      tableWithSearch:
        type: pages
        layout: table
        template: page
        search: true

  cardSizes:
    sections:
      cardsTiny:
        type: pages
        layout: cards
        size: tiny
        template: page

      cardsSmall:
        type: pages
        layout: cards
        size: small
        template: page

      cardsMedium:
        type: pages
        layout: cards
        size: medium
        template: page

      cardsLarge:
        type: pages
        layout: cards
        size: large
        template: page

  imageSettings:
    sections:
      cover:
        type: pages
        layout: cards
        image:
          cover: true
        template: page
      backBlack:
        type: pages
        layout: cards
        image:
          back: black
        template: page
      backWhite:
        type: pages
        layout: cards
        image:
          back: white
        template: page
      ratio4by5:
        type: pages
        layout: cards
        image:
          ratio: 4/5
          cover: true
        template: page
      ratio16by9:
        type: pages
        layout: cards
        image:
          ratio: 16/9
          cover: true
        template: page
  invalid:
    sections:
      invalid:
        type: pages
        min: 2
        template: does-not-exist

  pagination:
    sections:
      pagination:
        type: pages
        limit: 1
      paginationAndHelp:
        type: pages
        limit: 1
        help: This is some nice help for this pages section
      paginationNoDetails:
        type: pages
        limit: 1
