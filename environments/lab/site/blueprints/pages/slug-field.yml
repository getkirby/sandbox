title: Slug

tabs:
  general:
    fields:
      slug: true
      placeholder:
        type: slug
        placeholder: Write something …
      icon:
        type: slug
        icon: text
      before:
        type: slug
        before: Before
      after:
        type: slug
        after: After
      beforeAndAfter:
        type: slug
        before: Before
        after: After
      beforeAndAfterAndIcon:
        type: slug
        before: Before
        after: After
        icon: edit
      disabled:
        type: slug
        disabled: true
      disabledWithValue:
        type: slug
        disabled: true
      help:
        type: slug
        help: Some help text

  validation:
    fields:
      required:
        type: slug
        required: true
      maxlength:
        type: slug
        maxlength: 10
      minlength:
        type: slug
        maxlength: 10

  widths:
    fields:
      oneHalf1:
        type: slug
        width: 1/2
      oneHalf2:
        type: slug
        width: 1/2
        before: Before
        after: After
        placeholder: Placeholder
      oneThird1:
        type: slug
        width: 1/3
      oneThird2:
        type: slug
        width: 1/3
      oneThird3:
        type: slug
        width: 1/3
        before: Before
        after: After
        placeholder: Placeholder
      oneQuarter1:
        type: slug
        width: 1/4
      oneQuarter2:
        type: slug
        width: 1/4
      oneQuarter3:
        type: slug
        width: 1/4
      oneQuarter4:
        type: slug
        width: 1/4
        before: Before
        after: After
        placeholder: Placeholder

  preview:
    fields:
      structurePreview:
        type: structure
        fields:
          slug: true
      objectPreview:
        type: object
        fields:
          slug: true
