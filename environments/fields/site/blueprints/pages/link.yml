title: Link
extends: pages/default
columns:
  - width: 1/2
    fields:
      link: true
      placeholder:
        type: link
        placeholder: Link placeholder …
      before:
        type: link
        before: Before
      after:
        type: link
        after: After
      beforeAndAfter:
        type: link
        before: Before
        after: After
      beforeAndAfterAndIcon:
        type: link
        before: Before
        after: After
        icon: edit
  - width: 1/2
    fields:
      required:
        type: link
        required: true
      disabled:
        type: link
        disabled: true
      help:
        type: link
        help: Some help text
